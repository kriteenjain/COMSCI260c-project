torch>=2.2
transformers>=4.44
accelerate>=0.33
datasets>=2.20
tqdm>=4.66

# Compression backends (only needed for run_qwen_compression.py).
# Wanda has no extra deps -- it uses torch + transformers + datasets.
# AWQ requires CUDA; install on Colab/GPU only:
#   pip install autoawq
# Pinned >=0.2.6 because that's the first release with Qwen2.5 support.
# autoawq>=0.2.6
